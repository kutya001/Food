# Выполнено № 020: Исправление ошибки экспорта seed.js и устранение 404 favicon.ico

**Дата и время:** 2026-08-18 11:26

## Что было исправлено:

1. **Исправлен экспорт в `js/state/seed.js`:**
   * Добавлен именованный экспорт `export const SeedService = { getInitialData, seedAll }` для полной обратной совместимости.

2. **Очищен импорт в `js/views/dbViewerView.js`:**
   * Убран лишний импорт `SeedService`.
   * Кнопка сброса базы теперь напрямую вызывает стандартный метод СУБД `db.reset()`.

3. **Устранена ошибка 404 favicon.ico:**
   * В `index.html` добавлен тег `<link rel="icon" href="data:image/svg+xml,...">` с векторным эмодзи-логотипом 🍲.
   * Создан физический файл `favicon.svg` в корне проекта.
   * Браузер теперь мгновенно получает иконку без лишних сетевых запросов и 404-ошибок.
