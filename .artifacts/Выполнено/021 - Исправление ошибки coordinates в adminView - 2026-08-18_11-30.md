# Выполнено № 021: Исправление ошибки TypeError: Cannot read properties of undefined (reading 'lat') в adminView.js

**Дата и время:** 2026-08-18 11:30

## Что было исправлено:

1. **Безопасный рендеринг заведений общепита в `js/views/adminView.js`:**
   * Устранено ошибочное прямое обращение к `est.coordinates.lat`.
   * Колонка заменена на безопасный вывод контактов и режима работы заведения (`📞 Телефон`, `⏰ Время работы`).
   * Добавлена гибкая проверка статуса заведения (`open` / `active` / `suspended`).

2. **Безопасный рендеринг корпоративных клиентов в `js/views/adminView.js`:**
   * Добавлена поддержка всех вариантов полей: `monthlyLimit || budgetMonthly`, `employeesCount || employeeCount`, `contactPerson || address`.
   * Пополнение баланса корректно обновляет `currentBalance` в базе данных.

3. **Синхронизация и пуш:**
   * Изменения сохранены и отправлены в репозиторий GitHub.
