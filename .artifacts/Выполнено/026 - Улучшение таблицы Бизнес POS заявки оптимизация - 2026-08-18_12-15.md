# Выполнено № 026: Улучшение таблицы в разделе «Бизнес», аудит POS-терминала, расширенный жизненный цикл заявок и оптимизация производительности

**Дата и время:** 2026-08-18 12:15

## Что было сделано и исправлено:

1. **Таблица сырья/склада в разделе «Бизнес» ([js/views/businessWarehouseView.js](file:///c:/Users/OmuralievK/Desktop/MyFiles/Наработки/Antigravity/Food/js/views/businessWarehouseView.js), [js/state/seed.js](file:///c:/Users/OmuralievK/Desktop/MyFiles/Наработки/Antigravity/Food/js/state/seed.js), [js/state/db.js](file:///c:/Users/OmuralievK/Desktop/MyFiles/Наработки/Antigravity/Food/js/state/db.js)):**
   * Устранены `null`, `undefined` и `NaN` за счет унификации полей остатков (`currentStock` / `stockQty`) и минимальных порогов (`minStockAlert` / `minStockQty`).
   * Встроена авто-нормализация существующих записей в `db.init()`.
   * Добавлены KPI-карточки склада (Оценка запасов в сомах, всего наименований, требуют закупки).
   * Четкие статусные бейджи: `В НОРМЕ`, `МАЛО`, `КРИТИЧНО`.
   * Форматирование всех цен и сумм в сомах с разделителями тысяч.

2. **POS-терминал ([css/components/pos.css](file:///c:/Users/OmuralievK/Desktop/MyFiles/Наработки/Antigravity/Food/css/components/pos.css)):**
   * Исправлена сетка блюд `.pos-touch-grid`: добавлено `align-content: start;` и `grid-auto-rows: 150px;`, чтобы плитки не вытягивались в вертикальные столбы и сохраняли компактный, эргономичный размер для тач-экранов.

3. **Жизненный цикл корпоративных B2B заявок ([js/views/corporatePortalView.js](file:///c:/Users/OmuralievK/Desktop/MyFiles/Наработки/Antigravity/Food/js/views/corporatePortalView.js), [js/views/businessCorpRequestsView.js](file:///c:/Users/OmuralievK/Desktop/MyFiles/Наработки/Antigravity/Food/js/views/businessCorpRequestsView.js), [js/components/corpOrderModal.js](file:///c:/Users/OmuralievK/Desktop/MyFiles/Наработки/Antigravity/Food/js/components/corpOrderModal.js), [js/components/corpDetailsModal.js](file:///c:/Users/OmuralievK/Desktop/MyFiles/Наработки/Antigravity/Food/js/components/corpDetailsModal.js)):**
   * Реализована полная цепочка статусов:
     * `new` (⏳ Новая)
     * `accepted` (🤝 Принята)
     * `cooking` (👨‍🍳 Готовится)
     * `ready` (🍱 Готов)
     * `on_way` (🚚 В пути)
     * `delivered` (🎉 Доставлен)
     * `cancelled` (❌ Отменён)
   * **Для заявителя (Компания):**
     * Возможность **отменить заявку** в 1 клик со 100% возвратом средств на корпоративный баланс.
     * Возможность **редактировать заявку** (кнопка «✏️ Изменить»), пока она в статусе `new`, с динамическим пересчетом бюджета.
   * **Для заведения (Общепит):**
     * Быстрые кнопки перевода статусов («Принять», «На кухню», «Готово», «В путь», «Доставлено») и отклонения.

4. **Оптимизация производительности (Performance):**
   * Устранено накопление глобальных слушателей событий в `Header` и `MobileNav` через флаг `isListenersInitialized`.
   * Переработано панорамирование карты в `MapComponent`: события мыши/тача вешаются только на время перетаскивания и удаляются при отпускании, а отрисовка кадров синхронизирована через `requestAnimationFrame`.
