# Запрос № 014: Реализация Этапа 5 — Сенсорный POS-терминал кассира, Сквозное списание сырья и Кассовые смены

**Дата и время:** 2026-08-18 11:09

## Текст запроса пользователя
> выполняй

## Цели:
1. Создать стили `css/components/pos.css` для сенсорного кассового терминала, кнопок купюр и фискального чека.
2. Реализовать сервис сквозного списания сырья со склада `js/services/stockService.js`.
3. Реализовать модальные окна `js/components/posPaymentModal.js` (оплата и сдача в сомах), `js/components/posShiftModal.js` (смены и X/Z отчёты) и `js/components/posReceiptModal.js` (фискальный чек).
4. Реализовать полнофункциональный сенсорный экран кассира `js/views/posView.js` с авто-темой `neo-dark`.
5. Интегрировать модуль в `index.html` и `js/app.js`, обновить `ROADMAP.md` (до 75%), зафиксировать отчет в `Выполнено` и запушить в GitHub.
