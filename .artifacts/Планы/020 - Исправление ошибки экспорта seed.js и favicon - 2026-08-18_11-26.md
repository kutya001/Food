# План № 020: План исправления ошибки экспорта seed.js и добавления favicon

**Дата и время:** 2026-08-18 11:26

---

## План действий:
1. **Шаг 1:** В `js/state/seed.js` добавить экспорт объекта `SeedService` с методом `seedAll: () => INITIAL_SEED_DATA`.
2. **Шаг 2:** В `js/views/dbViewerView.js` убрать некорректный импорт и вызывать стандартный метод СУБД `db.reset()` при сбросе к Seed Data.
3. **Шаг 3:** В `index.html` добавить тег `<link rel="icon" ...>` с векторной SVG иконкой (`data:image/svg+xml,...`), чтобы браузер не запрашивал несуществующий `/favicon.ico`.
4. **Шаг 4:** Зафиксировать выполнение в `.artifacts/Выполнено/` и запушить исправления на GitHub.
