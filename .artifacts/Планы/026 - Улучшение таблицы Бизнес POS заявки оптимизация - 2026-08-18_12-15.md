# План № 026: План улучшений таблицы «Бизнес», POS-терминала, статусов заявок и оптимизации производительности

**Дата и время:** 2026-08-18 12:15

---

## Детальный план работ:

### 1. Улучшение таблицы сырья в разделе «Бизнес» (`js/views/businessWarehouseView.js`, `js/state/seed.js`, `js/state/db.js`):
* Нормализовать данные: заполнить и поддержать все свойства `currentStock` / `stockQty`, `minStockAlert` / `minStockQty`, `purchasePrice` / `costPrice`.
* Устранить `null`, `undefined` и `NaN` во всех расчетах и отображении сумм запаса в сомах.
* Сделать таблицу премиальной, компактной с бейджами «В НОРМЕ», «МАЛО», «КРИТИЧЕСКИЙ ЗАПАС» и авто-калькуляцией итогового баланса склада.

### 2. Исправление и оптимизация POS-терминала (`css/components/pos.css`, `js/views/posView.js`):
* Добавить `align-content: start;` и фиксированную высоту `grid-auto-rows: 140px;` для `.pos-touch-grid`, чтобы плитки блюд не вытягивались в вертикальные столбы, а были компактными и удобными для нажатий.
* Проверить работу скидок, кассовых смен X/Z, модалок оплаты наличными/картой в сомах и списания сырья со склада.

### 3. Полный жизненный цикл корпоративных B2B-заявок (`corpRequests`):
* Внедрить расширенную цепочку статусов:
  1. `new` («⏳ Новая»)
  2. `accepted` («🤝 Принята заведением»)
  3. `cooking` («👨‍🍳 Готовится на кухне»)
  4. `ready` («🍱 Готово к выдаче»)
  5. `on_way` («🚚 В пути / Доставка»)
  6. `delivered` («🎉 Доставлено / Выдано»)
  7. `cancelled` («❌ Отменена»)
* **Возможности заявителя (Организация):**
  * Возможность **отменить** заявку (кнопка «Отменить заявку»), пока статус `new`.
  * Возможность **редактировать** заявку (кнопка «✏️ Редактировать заявку»), пока статус `new` (изменение блюд, отделов, времени и примечаний).
* **Возможности заведения (Общепит):**
  * Перевод статусов `Принять` $\rightarrow$ `На кухню` $\rightarrow$ `Готово` $\rightarrow$ `В путь` $\rightarrow$ `Доставлено` или `Отклонить`.

### 4. Оптимизация производительности и устранение подтормаживаний:
* Устранить дублирование глобальных слушателей событий `window.addEventListener` в `MapComponent`, `Header`, `MobileNav`, которые накапливались при каждом рендере.
* Вынести регистрацию глобальных событий в `init()` методы.
* Добавить `requestAnimationFrame` для операций панорамирования холста карты.
